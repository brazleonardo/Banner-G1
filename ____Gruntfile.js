module.exports = function (grunt) {
    grunt.initConfig({

	grunt.initConfig({
		concat: {
			css: {
				src: "src/css/*.css",
				dest: "dist/css/app.css"

			},
			js: {
		 		src: "src/js/*.js",
		 		dest: "dist/js/app.js"
			}
		}
	});
};